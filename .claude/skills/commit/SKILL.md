# Commit Skill
1. Run `git status` and `git diff --stat`
2. Stage ALL changes including project.godot and *.md status files
3. Write a conventional commit message grouped by concern
4. After commit, run `git status` again to verify clean tree
5. Push to origin
